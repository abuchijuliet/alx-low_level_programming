#include <stdio.h>
#include "main.h"

/**
* _isupper - checks Uppercase or Lowercase
*@c:the value of c will be checked
*Return: 0 for lowercase or 1 for uppercase
*/
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
