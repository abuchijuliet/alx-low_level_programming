#include <stdio.h>
#include "main.h"

/**
* _isdigit - Checks for a digit 0 through 9
*@c:argument holder
*Return: for a digit 1 otherwise 2
*/
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
